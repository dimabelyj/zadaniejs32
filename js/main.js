let num1 = 2;
let num2 = 5;

function por(num1, num2) {
    if (num1 > num2) {
        // console.log(x);
        return (num1);

    } else if (num1 < num2) {
        //console.log(y);
        return (num2);
    }
}
console.log(por(num1, num2));
//por(num1, num2);